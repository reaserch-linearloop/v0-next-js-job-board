# Use legacy peer deps to handle conflicts
legacy-peer-deps=true

# Ensure node-linker is set to hoisted for better compatibility
node-linker=hoisted

# Disable strict peer dependencies
strict-peer-dependencies=false

# Force npm to install devDependencies
production=false
